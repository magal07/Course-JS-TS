function recursive(max) {
  console.log(max);
  if (max >= 957) return;
  max++;
  recursive(max);
}

recursive(0); // -10

// recursividade é uma função que chama a si mesma até que uma condição de parada seja atingida
// é importante ter uma condição de parada para que a função não entre em loop infinito
// a recursividade é muito utilizada em algoritmos de busca e ordenação
// é importante ter um caso base para a recursividade
// a recursividade pode ser mais lenta que um loop
// a recursividade pode ser mais fácil de entender que um loop
// a recursividade pode ser mais fácil de implementar que um loop
// a recursividade pode ser mais fácil de depurar que um loop
// a recursividade pode ser mais fácil de manter que um loop
// a recursividade pode ser mais fácil de ler que um loop
// a recursividade pode ser mais fácil de escrever que um loop
// a recursividade pode ser mais fácil de testar que um loop
// existe um limite de recursividade em JavaScript

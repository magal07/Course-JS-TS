// //      i = index    incremento
// for(let i = 500; i>=400; i-= 10 ){
//     console.log(`Linha: = ${i}`); // i+= 10, pra pular de 10 em 10
// }

        /* par ou impar
    for(let i = 0; i<=10; i++){
    const par = i % 2 === 0 ? 'par' : 'impar'; 
        console.log(i, par)
    } */

        // buscando arrays no laço for

 const frutas = ['Maça', 'Pera', 'Uva', 'Teste'];
 
 for (let i = 0; i < frutas.length; i++){ 
        console.log(`Índice: ${i} =`, frutas[i]);
 }
